{
    "name": "devsandbox",
    "private": true,
    "workspaces": [
      "apps/*",
      "packages/*"
    ],
    "scripts": {
      "start": "pnpm --parallel --stream start",
      "dev": "pnpm --parallel --stream dev",
      "build": "pnpm --parallel --stream build"
    },
    "devDependencies": {
      "typescript": "^5.0.0"
    }
  }